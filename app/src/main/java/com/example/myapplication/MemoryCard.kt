package com.example.myapplication

//sets default booleans for each button casted as a memory card
data class MemoryCard(var identifier: Int, var isFaceUp: Boolean=false, var isMatched: Boolean=false)